server:
  port: 8888
spring:
  datasource:
    url: jdbc:mysql://192.168.6.232/mes_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: zlbc_mes_db
    password: UejJGThZp3cv
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
logging:
  level:
    com.zlbc.CyUpErp.mapper: INFO
    java.sql: INFO
    org.apache.ibatis: INFO
